({
    removeFromOrder: function (component, event, helper) {
        helper.removeFromOrder(component);
    },

    fireUpdateItemQuantityEvent: function (component, event, helper) {
        helper.fireUpdateItemQuantityEvent(component, event);
    }
});