/**
 * Created by MaxymMirona on 18.12.2019.
 */

({
    onInit : function (component, event, helper){
      helper.doInit(component, event);
    },

    onHandleClick : function (component, event, helper) {
        helper.doHandleClick(component, event);
    }
});